setlocal nowrap                        " don't wrap lines
setlocal tabstop=4                     " a tab is two spaces
setlocal shiftwidth=4                  " an autoindent (with <<) is two spaces
setlocal expandtab                     " use spaces, not tabs
